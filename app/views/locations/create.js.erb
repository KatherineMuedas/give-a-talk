$("#location-modal").html('');
$("<%= escape_javascript (render 'locations/location_modal_content', new_location: @new_location, locations: @locations) %>").appendTo("#location-modal");
$(function() {
  $(document).foundation();
});
