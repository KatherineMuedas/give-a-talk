# Add account credentials and API keys here.
# See http://railsapps.github.io/rails-environment-variables.html
# This file should be listed in .gitignore to keep your settings secret!
# Each entry sets a local environment variable and overrides ENV variables in the Unix shell.
# For example, setting:
# GMAIL_user_name: Your_Gmail_user_name
# makes 'Your_Gmail_user_name' available as ENV["GMAIL_user_name"]

# Add application configuration variables here, as shown below.
#
MANDRILL_user_name: Your_user_name
MANDRILL_API_KEY: Your_API_Key
ADMIN_NAME: First User
ADMIN_EMAIL: user@example.com
ADMIN_PASSWORD: changeme
DEVISE_SECRET_KEY: rake secret
SECRET_KEY: rake secret
SITE_HOST: example.com
ROLES: [admin, user, VIP]
